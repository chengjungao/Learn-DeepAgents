; npm 10.x 的 arborist 在解析 vitest 4.x 的 peer 依赖环时会崩溃
; （TypeError: Cannot read properties of null (reading 'edgesOut')）。
; 这里跳过 peer 解析以绕过该 bug。前端依赖（@langchain/core、react）
; 均在 package.json 中显式声明，跳过 peer 不影响运行。
legacy-peer-deps=true
